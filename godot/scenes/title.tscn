; The front door. Until now the game had none: it opened straight onto a job board, which is like
; walking into a workshop and finding the shift already started.
;
; Hand-written text, reviewable in a diff. That is why we are in this engine.

[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/title.gd" id="1_title"]

[node name="Title" type="Control"]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1_title")
