[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]
; Epic's experimental MCP server, in-process in the editor. Lets an agent drive a RUNNING editor
; over HTTP instead of relaunching it for every change -- which is what most of the friction in
; this project has been: a cold start plus shader compilation per iteration.
bAutoStartServer=True
ServerPortNumber=8000
ServerUrlPath=/mcp
bEnableToolSearch=True
