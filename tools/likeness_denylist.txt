# Rule 16 — names and phrases that must never appear anywhere in this repository,
# including code, comments, asset filenames, string tables and commit messages.
#
# See docs/05-legal/ip-and-likeness.md. This is a blocking gate, not a suggestion.
#
# FORMAT: one term per line, case-insensitive. Blank lines and # comments ignored.
# Add the surname, the full name, and any distinctive catchphrase associated with
# a real person connected to the subject matter.
#
# ---------------------------------------------------------------------------
# This file is deliberately EMPTY in the public repository.
#
# Writing the terms here would publish the very association the policy exists to
# avoid, and would make the repo itself a search hit for them. The real list is
# maintained locally and privately in:
#
#     tools/likeness_denylist.local.txt        (gitignored)
#
# check_conventions.py reads both files and concatenates them. Populate the local
# file before M0; the check is inert until you do, and it says so when it runs.
# ---------------------------------------------------------------------------
