# Blueprints that are allowed to exist, with the reason.
#
# AGENTS.md: Blueprints are glue only. No Blueprint may tick, and no Blueprint may
# contain a gameplay decision — if it has an `if` about game rules, it belongs in
# SteeplejackSim, where it can be diffed, reviewed and tested.
#
# FORMAT:  Content/Path/To/BP_Thing.uasset    # why this must be a Blueprint
#
# The review friction is the control. Adding a line here should require someone to
# justify it out loud. An empty file is the healthy state.
